unit uEnums;

interface

type
  TStatus             = (Cadastrado, Encerrado, Cancelado, Excluido);
  TTipoMovimentacao   = (Normal, Reserva);
  TTipoProduto        = (Produto, Servico);
  TTipoOrdem          = (Compra, Venda);

  implementation

end.